for (
  // 1: declare and assign the `step` variable
  let step = 0; // initiation
  // 2, 5, 8, 11: check if `step` is less than 3
  step < 3; // condition
  // 4, 7, 10: add 1 to `step`
  step++ // step
) {
  // 3, 6, 9: log the current value of `step`
  console.log(step);
}

// 12: done
console.log("done");
